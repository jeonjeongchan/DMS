@model LoginModel

@{
    ViewData["PageName"] = "account_login";
}


@section Header {
    <link rel="stylesheet" media="screen, print" href="~/css/fa-brands.css">
}

<!--<div class="col-sm-12 col-md-6 col-lg-5 col-xl-4 mx-auto">
    <h1 class="text-white fw-300 mb-3 d-sm-block d-md-none">
        Secure login
    </h1>
    <div class="card p-4 rounded-plus bg-faded">
        <form id="js-login" asp-controller="Account" asp-action="Login" method="post">
            <div asp-validation-summary="All" class="alert alert-primary text-dark"></div>
            <div class="form-group">
                <label class="form-label" asp-for="MEMBER_ID">아이디</label>
                <input type="MEMBER_ID" asp-for="MEMBER_ID" class="form-control" placeholder="아이디" value="" required="required" />
                <span class="invalid-feedback" asp-validation-for="MEMBER_ID">아이디가 틀렸습니다.</span>
            </div>
            <div class="form-group">
                <label class="form-label" asp-for="PASSWORD">비밀번호</label>
                <input type="PASSWORD" asp-for="PASSWORD" class="form-control" placeholder="비밀번호" value="" required="required" />
                <span class="invalid-feedback" asp-validation-for="Password">비밀번호가 틀렸습니다.</span>
            </div>

            
            <div class="row no-gutters">
                <div class="col-lg-12">
                    <button type="submit" class="btn btn-info btn-block btn-lg">로그인</button>
                </div>

            </div>-->
@*             <div class="text-center">          
                <a asp-page="ForgotPassword" class="opacity-90">비밀번호를 잊어버렸나요?</a>        
            </div> *@
        <!--</form>
    </div>
</div>-->

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $("#js-logsin-btn").click(function (event) {
            // Fetch form to apply custom Bootstrap validation
            var form = $("#js-login");

            if (form[0].checkValidity() === false) {
                event.preventDefault();
                event.stopPropagation();
            }

            form.addClass('was-validated');
            // Perform ajax submit here...
        });
    </script>
}
